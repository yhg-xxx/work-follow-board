# 看板管理 + 置顶 + 手动拖拽排序 改造方案

## 一、概述（Summary）

把「临时专项」从"模块名含『临时』派生的聚合分组"升级为**真实看板**，并引入 **t_board 看板表**支持未来动态新增看板；同时为卡片增加 **置顶（pinned）** 与 **手动拖拽排序（sort_order）** 两项能力，全部持久化到数据库。

用户已确认的决策：
1. 建 `t_board` 表，临时专项作为真实看板（code=`temp`、名称`临时专项`、前缀`LS`、配色沿用紫色 `#8B5CF6`），现有 3 条临时事项迁移过去；未来新增看板无需改代码。
2. 置顶 + 拖拽手动排序并存：置顶**始终生效**（任何排序下置顶项排最前）；拖拽**仅在「手动排序」模式下启用**（排序下拉新增第 5 项）；切到其他排序时 `sort_order` 保留在库中，切回手动排序完全恢复。
3. 看板管理入口：**侧栏「看板管理」按钮 → 悬浮弹窗**（新增/编辑/删除/上移下移/配色/前缀）。
4. 删除规则：**有事项的看板禁止删除**；`quanfa / happy / temp` 为系统看板（`system_flag=1`）禁止删除但可改配置；空看板可删。
5. 备份 JSON：**动态按键分组** `{data:{quanfa:[], happy:[], temp:[], ...}}`，旧文件（quanfa/happy 两键）仍可导入。

## 二、现状分析（Current State Analysis）

### 看板现状（硬编码、派生式）
- `t_task.board` 仅有 `quanfa`/`happy` 两个真实值；「临时事项」靠**模块名包含"临时"**派生（后端 [TaskService.menuStats](file:///c:/Users/Administrator/Desktop/TMO/backend/src/main/java/com/example/task/service/TaskService.java#L106-L114) `TEMP_MODULE_HINT`、前端 [taskShared.ts](file:///c:/Users/Administrator/Desktop/TMO/front/src/utils/taskShared.ts#L12-L13)）。
- 数据库 3 条临时事项（id 84-86，QF-H01/H02/H03）实为 `board='quanfa'`、`module='临时专项'`。
- 卡片身份色由 [TaskList.vue boardKey()](file:///c:/Users/Administrator/Desktop/TMO/front/src/views/TaskList.vue#L93-L96) 判断（模块含"临时"→紫 / happy→橙 / 其余→蓝）。
- 硬编码点：后端 `BOARDS`/`ID_RE`/`nextCode` 前缀（QF/HF）、`MenuStatsDtos` 的 `temp` 字段；前端 `BOARDS` 常量、`GROUP_META`、`TEMP_MODULE_HINT`、`NavGroupId` 联合类型、`SidebarNav` 三个写死分组与底部统计、`normalizeBoard`、导入导出枚举校验、备份 JSON 固定 `{quanfa,happy}` 键。

### 排序/筛选/分页现状
- 排序：默认「截止日期正序」走后端 `sortOrder`（null 排最后）；优先级/更新时间走前端 JS 二次排序（[TaskList.vue L191-256](file:///c:/Users/Administrator/Desktop/TMO/front/src/views/TaskList.vue#L191-L256)）。
- 筛选：看板/状态/负责人/日期走后端（[TaskRepository.search](file:///c:/Users/Administrator/Desktop/TMO/backend/src/main/java/com/example/task/repository/TaskRepository.java#L23-L43)）；关键词前端高亮。
- 分页：前端无限滚动，`visibleTasks = tasks.slice(0, pageCount)`（每页 20）。

### 数据库（tmo_task.sql / init.sql）
- `tmo_task.sql` 是 DBX 导出的**当前库快照**（含数据），非建库脚本；真正的建库脚本是 [init.sql](file:///c:/Users/Administrator/Desktop/TMO/backend/src/main/resources/sql/init.sql)，历次结构变更走 migration 脚本（如 `migration_20260807_add_op_log.sql`）。
- `t_task.board` 为 `varchar(32)`，**无需改列**即可容纳新看板；需要的是新表、新列和数据迁移。

## 三、变更方案（Proposed Changes）

### 3.1 数据库

**更新 `backend/src/main/resources/sql/init.sql`**：
- 新增 `t_board` 表 + 3 行种子数据（quanfa/happy/temp）。
- `t_task` 增加 `pinned`、`sort_order` 两列。

**新增 `backend/src/main/resources/sql/migration_20260814_board_pin_sort.sql`**（一次性迁移，遵循现有 migration 惯例；INSERT 用 `ON DUPLICATE KEY UPDATE` 可重入）：

```sql
-- 1) 看板表（支持未来动态新增看板）
CREATE TABLE IF NOT EXISTS t_board (
  id          BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键',
  code        VARCHAR(32)  NOT NULL COMMENT '看板代码(唯一，如 quanfa/happy/temp)',
  name        VARCHAR(64)  NOT NULL COMMENT '看板名称(如 全发/会幸福/临时专项)',
  accent      VARCHAR(16)  NOT NULL DEFAULT '#2B59C3' COMMENT '身份色(十六进制)',
  prefix      VARCHAR(8)   NOT NULL DEFAULT 'QF' COMMENT '事项ID前缀(如 QF/HF/LS，1-2位大写字母)',
  sort_order  INT          NOT NULL DEFAULT 0 COMMENT '侧栏显示排序',
  system_flag TINYINT(1)   NOT NULL DEFAULT 0 COMMENT '系统看板(1=禁止删除)',
  created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  UNIQUE KEY uk_board_code   (code),
  UNIQUE KEY uk_board_prefix (prefix)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='看板表';

INSERT INTO t_board (code, name, accent, prefix, sort_order, system_flag) VALUES
('quanfa','全发',   '#2B59C3','QF',0,1),
('happy','会幸福',  '#E8862C','HF',1,1),
('temp', '临时专项','#8B5CF6','LS',2,1)
ON DUPLICATE KEY UPDATE name=VALUES(name), accent=VALUES(accent);

-- 2) t_task 新增置顶/手动排序字段
ALTER TABLE t_task ADD COLUMN pinned     TINYINT(1) NOT NULL DEFAULT 0 COMMENT '置顶(1=置顶，任意排序下排最前)' AFTER risk;
ALTER TABLE t_task ADD COLUMN sort_order INT DEFAULT NULL COMMENT '手动排序位置(仅手动排序模式生效)' AFTER pinned;
CREATE INDEX idx_task_pinned ON t_task (pinned);

-- 3) 数据迁移：模块名含「临时」的事项归入临时专项看板（现 3 条）
UPDATE t_task SET board='temp' WHERE module LIKE '%临时%';
```

**`tmo_task.sql` 处理**：该文件是 DBX 导出产物。**不手工编辑**；migration 执行后由用户在本地 DBX 重新导出以获得新结构快照。本方案以 init.sql + migration 为准。

### 3.2 后端（backend/src/main/java/com/example/task/）

| 文件 | 改动 |
|---|---|
| `entity/Board.java` **新增** | `t_board` 实体（id/code/name/accent/prefix/sortOrder/systemFlag/createdAt）。 |
| `repository/BoardRepository.java` **新增** | `findAllByOrderBySortOrderAsc()`、`findByCode()`。 |
| `dto/BoardDtos.java` **新增** | `BoardItem(code,name,accent,prefix,sortOrder,systemFlag,taskCount)`；`BoardRequest(code?,name,accent,prefix,sortOrder?)`；`BoardReorderRequest(ids)`。 |
| `controller/BoardController.java` **新增** | `GET /api/boards`；`POST /api/boards`；`PUT /api/boards/{id}`（code 不可改）；`DELETE /api/boards/{id}`；`PUT /api/boards/reorder`。 |
| `service/BoardService.java` **新增** | list（含每看板 taskCount）、create（code/prefix 唯一性 + 格式校验）、update、delete（有事项/system_flag 禁删，400 明确文案）、reorder（按 ids 顺序写 sort_order）。 |
| `entity/Task.java` | 增加 `pinned`(boolean)、`sortOrder`(Integer) 字段。 |
| `dto/TaskDtos.java` | `TaskListItem`/`TaskDetail` 增加 `pinned`、`sortOrder`（`TaskRequest` **不加**，置顶走独立接口）。 |
| `controller/TaskController.java` | 新增 `PATCH /api/tasks/{id}/pin`（body `{pinned}`）、`PUT /api/tasks/reorder`（body `{ids}`）。 |
| `service/TaskService.java` | 见下方明细。 |
| `dto/MenuStatsDtos.java` | `GroupStat` 增加 `label/accent/prefix`；删除 `temp` 字段（`allCount` 保留）。 |

**TaskService 明细**：
1. **menuStats**：改为从 `t_board`（按 sort_order）+ `countByBoardModule` 动态构造 `groups`；删除 `TEMP_MODULE_HINT` 聚合逻辑。每个 group = `{id: code, label: name, accent, prefix, count, modules}`。
2. **nextCode**：前缀改从 BoardService 取（quanfa→QF / happy→HF / temp→LS / 新看板自定义）；`ID_RE` 放宽为 `^([A-Z]{1,2})-([A-Z])(\d+)$`，按"该看板的前缀"过滤旧码（temp 看板旧码 QF-H01~03 前缀不匹配，不参与新码统计，新码从 LS-A01 起）。已有事项 ID 不受影响。
3. **list 排序**：`buildDeadlineSort` 泛化为 `buildSort(sortOrder)`：
   - `manual` → `Sort.by(pinned desc, sortOrder asc NULLS_LAST, id asc)`
   - `asc/desc`（截止）→ `Sort.by(pinned desc, deadline asc/desc NULLS_LAST, id asc)`
4. **importBatch 校验**：board 合法性从静态 `BOARDS` 集合改为查 `t_board` 全部 code。
5. **新增方法**：`pin(id, pinned)`（写 pinned + updatedAt）；`reorder(ids)`（逐个写 `sort_order = 数组下标`，仅对传入 id 生效——已加载集合内的相对顺序）。
6. **toListItem/toDetail**：带上 pinned/sortOrder。
7. 静态 `BOARDS` 集合删除，改用 BoardRepository。

### 3.3 前端（front/src/）

**`api/task.ts`**：
- `TaskListItem`/`TaskDetail` 增加 `pinned: boolean`、`sortOrder: number | null`。
- `MenuStats` 类型：`MenuGroupStat` 增加 `label/accent/prefix`；删除 `temp` 字段。
- 新增 Board 类型与接口：`listBoards()`、`createBoard()`、`updateBoard()`、`deleteBoard()`、`reorderBoards(ids)`。
- 新增 `pinTask(id, pinned)`（PATCH）、`reorderTasks(ids)`（PUT）。

**`utils/taskShared.ts`**：
- 删除 `TEMP_MODULE_HINT`、静态 `BOARDS`、静态 `GROUP_META`（`all` 的特殊 meta 移入 TaskList 动态构造）。
- `NavGroupId` 改为 `string`（看板 code 或 `'all'`）。
- `MenuGroup` 保留结构（id/label/accent/prefix/boardFilters/count/modules），数据全部动态。
- `normalizeBoard(v, boardMap)`：优先中文名→code（如 全发→quanfa、临时专项→temp），已是合法 code 则直通，否则原样返回由导入校验报错。
- `boardLabel(b, boardMap)`：按 code 查名称，未知 code 回退 b 本身。

**新增 `components/BoardManagerDialog.vue`**（看板管理弹窗）：
- 仿 EditorDialog 的 el-dialog；受控组件，`expose open()`。
- 列表每行：色块（accent）+ 名称 + 前缀 + 事项数 + 「系统」标签 + 操作（上移/下移/编辑/删除）。
- 新增/编辑表单：代码（仅新增可填，`[a-z0-9_-]+` 唯一）、名称、前缀（1-2 位大写字母，默认按名称首字提示）、配色（预设色板 + 自定义 hex）。
- 删除：有事项或系统看板 → `ElMessage.warning` 明确文案（"该看板下有 N 项事项，不能删除" / "系统看板不可删除"）；空看板删除前 confirm。
- `emit('changed')` → 父组件刷新菜单统计与数据。

**`components/SidebarNav.vue`**：
- `menuGroups` 已是动态 props，模板 `v-for` 渲染（删注释中"三个一级分组"写死描述）；分组图标：quanfa/happy 保留专用 SVG，其余用星形 SVG（现有 `v-else`）。
- 底部统计条改为 `v-for` 动态渲染（现写死 3 行）。
- 新增「看板管理」入口按钮（导航底部 / 统计条上方，齿轮图标 + 文案），`emit('manage-boards')`。

**`components/FilterPanel.vue`**：
- 看板选项从静态 `BOARDS` 改为 **prop `boards: {label, value}[]`**（父组件由 boardMap 构造）。

**`components/EditorDialog.vue`**：
- `open()` 时拉取 `/boards`，看板选择从静态 `BOARDS` 改为动态列表；`boardPrefix()` 改为查 boardMap 的 prefix；`.board-card.quanfa/.happy` 选中态配色改为 inline accent（`--accent` 变量）。
- 新建时 `fetchNextCode(form.board, form.module)` 已传 board，后端已动态，无需改调用。

**`components/ImportDialog.vue`**：
- 打开时拉取 `/boards` 构建 boardMap；`normalizeBoard` 传 map；未知看板行按现有错误收集提示（列出合法看板名）。
- 备份 JSON 解析：按动态键分组导入（保留兼容：仅 quanfa/happy 键的旧文件仍可导入）。
- `MAIN_COLUMNS` 中 board 列去掉静态 `enumValues`（导入时按动态看板列表校验）。

**`views/TaskList.vue`**（改动最大）：
1. **看板数据**：`boardMap = ref<Record<string, BoardItem>>`；onMounted 并行拉 `/boards` + menuStats；`menuGroups` 按 `menuStats.groups` 动态映射（label/accent/prefix 直接来自后端）。
2. **导航**：`navState.group` 类型放宽为 string；`'all'` 的 `boardFilters = 全部看板 code`；`buildQuery` 删除 temp 分支（临时看板与普通看板一致）。
3. **卡片身份色**：删除 `boardKey()`/`cardClass()` 的临时判断；卡片改为 `:style="{ '--spine': accent, '--tab-bg': accent }"`（footer 脊线 + tab 背景统一用看板 accent）；tab 的 title 提示用看板名称。
4. **排序**：`SORT_OPTIONS` 增加第 5 项「手动排序」；`SortField` 增加 `'manual'`；`tasks` computed：manual 直通后端顺序；其余排序 JS 排序时**先按 pinned 降序**再按原字段。
5. **置顶**：卡片 footer 新增图钉按钮（outline↔filled，`title="置顶/取消置顶"`），点击 `pinTask` 成功后 `reload()`；置顶卡片 tab 左侧加小图钉标记（`.card-tab.pinned::before` 用 CSS 绘制或 SVG）。
6. **拖拽**：安装 `sortablejs`（+ `@types/sortablejs`）；`watch` 手动排序模式：进入时 `new Sortable(cardsGridEl, { filter: 'button, input, .card-check', preventOnFilter: true, animation: 150 })` 并给卡片加 `.draggable` 类显示拖拽手柄（卡片右上角 ⋮⋮，仅手动模式可见）；退出时 `destroy()`。`onEnd`：在 `tasks.value` 全序数组中按可见区移动计算新顺序 → `reorderTasks(ids)` → `fetchFiltered()`。
7. **筛选默认值**：`filters.boards`/`draftFilters.boards` 默认值由 `['quanfa','happy']` 改为**全部看板 code**（动态）。
8. **导出**：`boardLabel(t.board, boardMap)`；备份 JSON 按 boardMap 动态按键分组 `{data:{...}}`。
9. **管理弹窗挂载**：`<BoardManagerDialog ref="boardMgrRef" @changed="reload" />`；`@manage-boards="boardMgrRef?.open()"` 传给 SidebarNav。

### 3.4 依赖

- `front/package.json`：新增 `sortablejs`、`@types/sortablejs`（dev）。

### 3.5 设计要点（frontend-design 对齐现有语言）

- **看板身份 = 颜色**：accent 色是全局签名元素，一处改色（管理弹窗）即同步侧栏图标、卡片 tab、底部脊线、编辑弹窗选中态，不做第二种装饰性表达。
- **置顶**：footer 小图钉图标按钮，保持现有紧凑 footer 风格（与「跟进/编辑」同一视觉层级），不新增横幅/色带。
- **拖拽手柄**：仅手动排序模式下在卡片右上角显示 ⋮⋮ 手柄，其余模式完全不可见，避免视觉噪音。
- **管理弹窗**：沿用 EditorDialog 的 el-dialog 与色块语言；新看板配色给 6 色预设色板 + 自定义输入，避免自由取色产生与现有体系冲突的突兀色。
- 空看板、禁删提示等文案按"写清楚为什么、怎么解决"的口径（如"该看板下有 N 项事项，先移动或删除事项后再删除看板"）。

## 四、关键决策记录（Assumptions & Decisions）

| # | 决策 | 说明 |
|---|---|---|
| 1 | `t_board` 独立表，temp 为真实看板 | 前端 `NavGroupId`、后端 `menuStats`、`nextCode` 全部动态化，删除"模块名含临时"派生逻辑（此后模块名含"临时"不再自动归类，归属完全由看板决定）。 |
| 2 | temp 前缀 = LS，旧码 QF-H01~03 保留 | 旧码不参与新码统计，temp 看板新码从 LS-A01 起；模块字母分配按看板独立。 |
| 3 | `sort_order` 为全局字段（非按看板作用域） | 手动排序在"全部看板"视图下仍可用；重排仅作用于当前已加载集合，未加载/被筛选掉的卡片 `sort_order` 不变，歧义由 `id asc` 兜底。48 条数据规模下可接受。 |
| 4 | 排序优先级：pinned 始终第一排序键 | 任意排序模式下置顶项排最前；手动模式下 pinned 优先于 sort_order。 |
| 5 | `TaskRequest` 不含 pinned | 置顶走独立 PATCH 接口，编辑弹窗不误清/不误改。 |
| 6 | `tmo_task.sql` 不手工编辑 | 导出产物，由 DBX 重导；本次变更以 init.sql + migration_20260814 为准。 |
| 7 | 导入看板校验动态化 | xlsx/csv/JSON 导入的看板列按 t_board 实时校验（兼容中文名与 code），未知看板行报错列出合法值。 |
| 8 | 操作日志：置顶记录 `logOp('UPDATE')`；拖拽重排不记录 | 避免拖拽时刷屏操作日志；如需可后续补批量文案。 |

## 五、实施顺序

1. **数据库**：更新 `init.sql` + 新增 `migration_20260814_board_pin_sort.sql`。
2. **后端**：Board 实体/Repo/DTO/Service/Controller；Task 实体加字段；TaskService（menuStats/nextCode/list排序/import校验/pin/reorder）；MenuStatsDtos/TaskDtos。
3. **前端数据层**：`api/task.ts`、`utils/taskShared.ts`；安装 sortablejs。
4. **前端组件**：SidebarNav、FilterPanel、EditorDialog、ImportDialog、新增 BoardManagerDialog。
5. **TaskList 集成**：看板动态化、卡片配色、排序（含手动模式）、置顶按钮、拖拽、导出动态化。
6. **验证**（见下）。

## 六、验证步骤（Verification）

1. **数据库**：执行 migration SQL 后 `SHOW COLUMNS FROM t_task` 含 pinned/sort_order；`SELECT * FROM t_board` 3 行；3 条临时事项 board 已变 temp。
2. **后端**：`cd backend && ./mvnw spring-boot:run` 启动无报错；`GET /api/boards` 返回 3 看板含 accent/prefix/taskCount；`GET /api/tasks/menu-stats` 返回动态 groups（无 temp 字段）。
3. **类型检查**：`cd front && npm run type-check` 通过。
4. **功能**（`npm run dev`）：
   - 侧栏出现「临时专项」真实分组（紫色）+ 「看板管理」入口；点临时专项看到 3 条迁移数据。
   - 新建事项：看板下拉含临时专项（前缀 LS），生成 LS-A01；选其他看板前缀正确。
   - 置顶：卡片 footer 图钉 → 刷新后仍置顶；切换任一种排序，置顶项都在最前。
   - 手动排序：选「手动排序」出现拖拽手柄，拖拽后刷新保持；切其他排序再切回，拖拽顺序恢复。
   - 看板管理：新增看板（代码/名称/色/前缀）→ 侧栏出现、新建可选、卡片配色生效；有事项的看板删除被拒并提示；空看板可删。
   - 导入导出：导出的 xlsx/csv「看板」列含临时专项；备份 JSON 含 temp 键且可回导；旧 JSON（quanfa/happy）仍可导入。
   - 回归：现有筛选/排序/跟进面板/操作日志/批量删除正常；深色模式下新组件样式正常。
